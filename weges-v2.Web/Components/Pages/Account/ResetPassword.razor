@page "/reset-password"
@using weges_v2.SharedKernel.Models

<h3>Reset Password</h3>
<EditForm Model="_resetPasswordModel" OnValidSubmit="ResetPasswordCall">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div>
        <label>New Password:</label>
        <InputText type="password" @bind-Value="_resetPasswordModel.NewPassword" />
    </div>

    <div>
        <label>Confirm Password:</label>
        <InputText type="password" @bind-Value="_resetPasswordModel.ConfirmPassword" />
    </div>

    <button type="submit">Reset Password</button>
</EditForm>

@code {
    private HttpClient _httpClient;
    private RegisterModel _registerModel = new RegisterModel();
    private LoginModel _loginModel = new LoginModel();
    private ManageInfoModel _manageInfoModel = new ManageInfoModel();
    private ForgotPasswordModel _forgotPasswordModel = new ForgotPasswordModel();
    private ResetPasswordModel _resetPasswordModel = new ResetPasswordModel();

    protected override async Task OnInitializedAsync()
    {
        _httpClient = new HttpClient();
    }

    private async Task RegisterUser()
    {
        var response = await _httpClient.PostAsJsonAsync("/api/register", _registerModel);

        if (response.IsSuccessStatusCode)
        {
            // Handle success (e.g., navigate to login page or display success message)
        }
        else
        {
            // Handle error (e.g., display error message)
        }
    }

    private async Task LoginUser()
    {
        var response = await _httpClient.PostAsJsonAsync("/api/login", _loginModel);

        if (response.IsSuccessStatusCode)
        {
            // Handle success (e.g., store token, navigate to another page)
        }
        else
        {
            // Handle error (e.g., display error message)
        }
    }

    private async Task UpdateInfo()
    {
        var response = await _httpClient.PostAsJsonAsync("/api/manage/info", _manageInfoModel);

        if (response.IsSuccessStatusCode)
        {
            // Handle success (e.g., display success message)
        }
        else
        {
            // Handle error (e.g., display error message)
        }
    }

    private async Task ResetPasswordCall()
    {
        var response = await _httpClient.PostAsJsonAsync("/api/resetPassword", _resetPasswordModel);

        if (response.IsSuccessStatusCode)
        {
            // Handle success (e.g., display success message)
        }
        else
        {
            // Handle error (e.g., display error message)
        }
    }

    public class RegisterModel
    {
        public string Email { get; set; } = string.Empty;
        public string Password { get; set; } = string.Empty;
    }

    public class ManageInfoModel
    {
        public string FirstName { get; set; } = string.Empty;
        public string LastName { get; set; } = string.Empty;
        public string Email { get; set; } = string.Empty;
    }
}